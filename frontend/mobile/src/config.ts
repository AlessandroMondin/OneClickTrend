// API_HOST is replaced by `make configure-api` with the Mac's LAN IP so a
// physical device can reach the backend. localhost works on the simulator.
export const API_URL = "http://10.6.67.79:3000";
